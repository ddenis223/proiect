<!DOCTYPE html>
<html lang="ro">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <header>
        <div class="container">
            <h1><a href="/">Trabajo Fácil</a></h1>
            <nav>
                <ul>
                    <li><a href="/">Acasă</a></li>
                    <% if (locals.isAuthenticated) { %>
                        <li><a href="/dashboard">Panou de control</a></li>
                        <li><a href="/logout">Deconectare (<%= locals.username %>)</a></li>
                    <% } else { %>
                        <li><a href="/login">Autentificare</a></li>
                        <li><a href="/register">Înregistrare</a></li>
                    <% } %>
                </ul>
            </nav>
        </div>
    </header>
    <main>
        <div class="container">
            <form action="/reset-password" method="POST">
                <h2>Resetează parola</h2>
                <% if (error) { %>
                    <p style="color: red; text-align: center; font-weight: bold;"><%= error %></p>
                <% } %>
                <% if (token) { %>
                    <input type="hidden" name="token" value="<%= token %>">
                    <div>
                        <label for="password">Parolă nouă:</label>
                        <input type="password" id="password" name="password" required>
                    </div>
                    <div>
                        <label for="confirmPassword">Confirmă parola nouă:</label>
                        <input type="password" id="confirmPassword" name="confirmPassword" required>
                    </div>
                    <button type="submit">Resetează parola</button>
                <% } else { %>
                    <p style="text-align: center;">Token de resetare lipsă sau invalid. Vă rugăm să cereți un nou link de resetare.</p>
                    <p><a href="/forgot-password">Cere un nou link de resetare</a></p>
                <% } %>
            </form>
        </div>
    </main>
    <footer>
        <div class="container">
            <p>&copy; 2025 Trabajo Fácil. Toate drepturile rezervate.</p>
        </div>
    </footer>
</body>
</html>
